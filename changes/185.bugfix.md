Fix `DefaultChunkKeyEncoding.decode_chunk_key` so it is the inverse of
`encode_chunk_key`. Previously, decoding a chunk key with non-empty coordinates
(e.g. `"c/0/1"`) left a leading empty token after stripping the `"c"` prefix,
raising `ValueError`.
