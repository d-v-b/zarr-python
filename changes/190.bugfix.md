Group iteration now forwards the child-consolidation flag through recursive and synchronous paths while preserving an already-consolidated root snapshot. Requiring an existing array without a dtype skips dtype validation. Group attribute updates merge consistently, timed-out synchronous calls cancel their coroutines, and GPU buffers initialize consistently with CPU buffers.
