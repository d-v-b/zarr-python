Opening a group through `zarr.open` reuses the metadata buffers fetched during array detection, avoiding duplicate reads of `zarr.json` and `.zattrs` while preserving consolidated-metadata selection.
