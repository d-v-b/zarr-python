Added CI-only tests that verify the sharding codec's inner-chunk offset arithmetic against layouts derived independently by the CuTe layout algebra (PyCuTe, a test-only dependency installed in the `optional` test matrix; never imported at runtime).
