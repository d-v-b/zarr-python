`Array` and `AsyncArray` now share one immutable `ArrayState` through a common state cell, so a change made through an `Array` or its `async_array` is seen by both. `Array` no longer delegates its IO to `AsyncArray`, and `update_attributes` no longer mutates the previous metadata in place.
