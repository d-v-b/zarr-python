Added `examples/serve_tiff.py`, which serves a TIFF file over HTTP as a Zarr
hierarchy by opening it with `tifffile` as a read-only `zarr.abc.store.Store`,
plus a user guide section on serving data that is not stored as Zarr.
