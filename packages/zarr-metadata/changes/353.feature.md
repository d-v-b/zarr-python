`must_understand: false` is refused at every extension point, codecs and
storage transformers included: ignoring a codec gives wrong bytes as
surely as ignoring a data type gives wrong values, and the spec naming
only three points is read as an oversight. The JSON schema
`zarr_metadata.pydantic` generates for an array document says the same.
